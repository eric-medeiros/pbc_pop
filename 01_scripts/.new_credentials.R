library(gitcreds)

# Vá em: Settings > Developer settings > Personal access tokens > Tokens(classic) > Generate new token > Generate new token (classic) > select  "gist, repo, user, workflow"
# Se já tiver, só regenerate token


# Entrar no menu
gitcreds_set()

# Opção replace these credentials
2

# Novo token
#--PASTE--
